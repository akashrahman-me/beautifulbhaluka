---
By following our architecture
In the `courier` screen, instead of section type titlebar, use our `screentopbar` reuable component

In the `courier` screen, implement `publish` screen where user will add restuarent details.

in the publish screen will have those field:

-   নাম
-   ঠিকানা
-   লোকেশন
-   মোবাইল নাম্বার
-   ছবি।

however, in the `courier` card will have a rating option so that anyone can rate this out of 5 star. we don't need to add any note in the rating time.

---
