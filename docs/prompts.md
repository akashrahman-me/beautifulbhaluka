By following our architecture
In the `Craftsman` screen, instead of section type titlebar, use our `screentopbar` reuable component

and implement `publish` screen where user will add details.

in the publish screen will have those field:

-   নাম
-   ঠিকানা
-   মোবাইল নাম্বার
-   ছবি,
-   মিস্ত্রির ধরন/ক্যাটাগরি

---

Entertainment screen seems doesn't impelemeneted it. please implement similar to the craftsmanscreen.
it will have only those type of category:

-   সাউন্ড সিস্টেমে
-   ব্যান্ড পার্টি

and those field in publish screen

-   নাম, ঠিকানা, মোবাইল নাম্বার, ছবি, ক্যাটাগরি

for UI, just use mock data with open source image url.

be sure you follow our architecture I attached and ensure design it ultra-modern, professional, visually clean with smooth spacing, consistent typography, clear hierarchy, intuitive navigation, modern colors, possible place use linear gradient. Use icons where possible and reuse existing components when available. And at the end you don't need to write any markdown documentation to explain.
