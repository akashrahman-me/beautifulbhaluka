By following our architecture
In the `shopping` screen, instead of section type titlebar, use our `screentopbar` reuable component

In the `shopping` screen, implement `publish` screen where user will add restuarent details.

in the publish screen will have those field:

-   নাম
-   ঠিকানা
-   লোকেশন
-   মোবাইল নাম্বার,
-   ছবি।

I have attached architecture so that you can understood about our project.
